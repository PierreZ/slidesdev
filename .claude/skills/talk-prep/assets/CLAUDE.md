# TALK_TITLE

## Context
- **Event**: EVENT, CITY, DATE. Accent `#HEX` (EVENT brand), footer `#EVENT_HASHTAG`.
- **Format**: DURATION min, about N slides, delivered in LANGUAGE, slides in English.
- **Audience**: who they are, how many know the core tech.
- **Source**: new talk, or reworked from `talks/OTHER/` / a previous event.

## Narrative spine: "ONE SENTENCE THESIS"
1. Cold open: STORY, told as ...
2. Why now: ...
3. Motif: DIAGRAM, states: neutral, then ..., then ...
4. ...
N. Bookend to STORY, then the ladder.

## Spoken beats (not on slides)
- Slide X: the black cat nickname, the driving test analogy.
- Slide Y: ...
Record stories, jokes, and polls Pierre carries verbally so the next rework knows why a slide is sparse.

## Evidence
- Belief → source → headline number, one per line.

## Reductions applied (if reworked)
- What was cut or merged, and why.

## Conventions
- No em dashes. No `v-click`, progressive reveals are separate slides. No presenter notes.
- Emoji map: 👤 users, 🌍 world, 🖥️ system, ... (one per concept, reused).
- Code: Java for app-level examples, Rust for his libraries.

## Images
Real assets present: `public/...`.
Placeholders to fill (dashed boxes in the deck): `/foo.png` (slide X), ...

## Commands
`nix develop ../.. --command pnpm install --ignore-scripts` then `pnpm dev` / `pnpm build` / `pnpm export`.
